export const DEFAULT_NAMESPACE_LABEL = 'root';

export const NAMESPACE_LABEL_REGEX = /^[-A-Za-z_/]+$/;

export const NAMESPACE_PATH_SEPARATOR = ':';

export const PATH_REGEX = /^[-A-Za-z_.]+$/;

export const PATH_SEPARATOR = '.';